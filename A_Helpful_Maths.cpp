#include <iostream>
using namespace std;

int main(){
    
    string sum;
    cin << sum;

    while(sum--){
        for(int i = 0; i < sum.size(); i++){
            for(int j = i+1; < sum.size(); j++){
                if(sum[j] == '+'){
                    j++
                }
                if(sum[j] < sum[i]){
                    sum[]
                }
            }
        }
    }
    
}